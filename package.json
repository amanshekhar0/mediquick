{
  "name": "mediequip",
  "private": true,
  "description": "MediEquip 2.0 — verify before push",
  "scripts": {
    "verify": "npm run build --prefix client && node --check server/src/index.js && node --check server/src/routes/triage.js",
    "build:client": "npm run build --prefix client",
    "install:all": "npm install --prefix client && npm install --prefix server"
  }
}
